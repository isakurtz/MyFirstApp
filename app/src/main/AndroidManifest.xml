<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.programa">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainMenuActivity" />
        <activity android:name=".JornadaActivity" />
        <activity android:name=".QuestionarioActivity" />
        <activity android:name=".TarefaScanActivity" />
        <activity android:name=".MapsWebView" />
        <activity
            android:name=".RankingActivity"
            android:label="Ranking"></activity>
        <activity
            android:name=".BadgesActivity"
            android:label="Badges"></activity>
        <activity android:name=".LogDeTarefasActivity" android:label="Log de Tarefas"></activity>
    </application>

</manifest>